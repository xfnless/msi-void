#!/usr/bin/env bash
# Update Void and the separately installed Helium browser.
set -euo pipefail
sudo xbps-install -Syu
script=$(readlink -f "$0")
repo=$(cd "$(dirname "$script")/../../../.." && pwd)
sh "$repo/90-helium.sh"
